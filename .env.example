# Database — same PostgreSQL as usp-e-ticaret (shared DB)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/usp_eticaret?schema=public"

# Railway PostgreSQL (production)
# DATABASE_URL="postgresql://USER:PASSWORD@containers-us-west-xxx.railway.app:6543/railway"

# NextAuth
NEXTAUTH_URL="http://localhost:3001"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Production (Railway)
# NEXTAUTH_URL="https://usp-erp-production.up.railway.app"
